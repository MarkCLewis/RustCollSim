
use crate::data::basic::Vector;

/*
 * Computes the time derivative of x_hat
 * Returns a new Vector
 */
fn x_hat_dot(x_hat: &Vector, delta_velocity: &Vector) -> Vector {
    /**
     * x_hat = [ delta vel - hat x (delta vel * hat x) ] / |x|
     * 
     */
}